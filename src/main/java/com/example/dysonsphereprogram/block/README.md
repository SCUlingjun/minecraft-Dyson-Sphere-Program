# block

存放所有自定义方块类（`Block` 子类）的根目录。

每个方块类负责定义方块的物理属性（硬度、爆炸抗性、声音、地图颜色）、碰撞形状，以及与方块状态（BlockState）相关的交互逻辑（如右键点击、踩踏触发）。

方块注册入口统一在 `registry/ModBlocks.java` 中完成，此目录只放方块行为实现。

子目录：
- `machine/` — 各类机器方块
- `structure/` — 多方块结构的组成部件方块
- `storage/` — 存储类方块（箱子、罐体等）
